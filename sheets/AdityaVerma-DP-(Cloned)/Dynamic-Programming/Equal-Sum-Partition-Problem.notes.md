# Equal Sum Partition Problem

just check if the sum is even cz for equal sum it has to be even yk
so after that just do it like subset sum hehe:) 
it eventually become subset sum yk if half is there another half will also be there