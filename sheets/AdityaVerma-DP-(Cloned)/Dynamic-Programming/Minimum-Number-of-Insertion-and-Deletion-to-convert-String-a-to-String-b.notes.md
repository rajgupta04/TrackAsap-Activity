# Minimum Number of Insertion and Deletion to convert String a to String b

Same like shortest common superSequence 
the idea here was figuring out that min insertion + deletion is nothing but len(s1) + len(s2) - lcs*2